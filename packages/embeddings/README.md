# @kg/embeddings

Knowledge Graph Embeddings module for the Knowledge Graph Desktop project.

Implemented algorithms (pedagogical MVP):

- TransE
- TransH
- TransR
- DistMult
- ComplEx

This package exposes:

- `KGEEngine` to train and compare algorithms.
- Deterministic runs through `seed`.
- A lightweight 2D projection output for UI comparison.
